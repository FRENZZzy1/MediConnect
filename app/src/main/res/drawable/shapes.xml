<?xml version="1.0" encoding="utf-8"?>


<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:type="linear"
        android:angle="270"
        android:startColor="@color/splash_gradient_top"
        android:endColor="@color/splash_gradient_bottom" />
</shape>




<!-- ── 3. shape_icon_card.xml ───────────────────────────────────
     Rounded-square card that holds the stethoscope icon.
     Frosted-glass look: semi-transparent fill + white border.
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/icon_card_bg" />
    <stroke
        android:width="1.5dp"
        android:color="@color/icon_card_stroke" />
    <corners android:radius="20dp" />
</shape>
-->

<!-- ── 4. shape_btn_get_started.xml ─────────────────────────────
     Pill-shaped white button (fully rounded ends).
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/btn_bg" />
    <corners android:radius="50dp" />
</shape>
-->

<!-- ── 5. ripple_btn_get_started.xml ────────────────────────────
     Ripple wrapper for the Get Started button.
     Reference this as the background instead of shape_btn_get_started.
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/btn_ripple">
    <item android:drawable="@drawable/shape_btn_get_started" />
</ripple>
-->